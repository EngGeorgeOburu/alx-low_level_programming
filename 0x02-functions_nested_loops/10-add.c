#include "main.h"
/**
 * main - Entry point of the program
 * add: program adds two intergers n and m
 * Return: total of n and m
 */
int add(int n, int m)
{
	int total;

	total = n + m;
	return (total);
}

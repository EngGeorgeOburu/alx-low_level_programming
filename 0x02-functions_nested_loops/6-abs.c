#include "main.h"
/**
 * Description:program that computes the absolute value 
 * of an integer
 * Return:Always the absolute value of int
 */
int _abs(int i)
{
	if (i >= O)
	{
		return (i);
	}
	return (-i);
}

#include "main.h"
/**
 * mul - This function multiples two numbers.
 *
 * @a: The first number to be multiplied.
 *
 * @b: The second number to be multiplied.
 *
 * Return:Always 0 (success).
 */
int mul(int a, int b)
{
	int total;

	total = a * b;
	return (total);
}

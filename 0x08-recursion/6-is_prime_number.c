#include "main.h"
/**
 * is_prime_number - return 1 is the prime number is true.
 * @n: input parameter.
 */
int is_prime_number(int n)
{
	if (n <= 1)
	{
		return (0);
	}
	if (n == 2)
	{
		return (1);
	}
	if (factor * factor > n )
	{
		return (1);
	}
	else (n % factor == 0)
	{
		return (0);
	}
}

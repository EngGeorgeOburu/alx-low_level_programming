# include "main.h"
#include <string.h>
/**
 * void print_rev - The function that reverses a string
 *
 * @s: String used.
 */
void print_rev( char *s)
{
	int i;
	int len = strlen(str);

	for (i = len - 1; i >= i--)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}

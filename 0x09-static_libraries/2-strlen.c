#include "main.h"
#include <string.h>
/**
 *_strlen - This function returns the lenth of a string.
 *
 * @s: String.
 *
 * Return: Always (0).
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
